// @flow
const PNG: 'png' = 'png';
const PDF: 'pdf' = 'pdf';
const XLSX: 'xlsx' = 'xlsx';

const FILE_VARIANTS = {
	PDF,
	PNG,
	XLSX
};

const TABLE_NAME_LENGTH_LIMIT = 31;

export {
	FILE_VARIANTS,
	TABLE_NAME_LENGTH_LIMIT
};
