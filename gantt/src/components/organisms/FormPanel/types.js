// @flow
export type ConnectedProps = {
};

export type Props = {
	endDate: string,
	errorSettings: boolean,
	loading: boolean,
	multiplicityCheckbox: boolean,
	resources: Object,
	setRangeTime: (date: Object) => any,
	settings: Object,
	startDate: string,
	viewOfNestingCheckbox: boolean
};
