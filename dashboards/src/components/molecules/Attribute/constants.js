// @flow

// Типы методов обработки значения атрибута
const AGGREGATION: 'AGGREGATION' = 'AGGREGATION';
const GROUP: 'GROUP' = 'GROUP';

const REF_INPUT_TYPES = {
	AGGREGATION,
	GROUP
};

export {
	REF_INPUT_TYPES
};
