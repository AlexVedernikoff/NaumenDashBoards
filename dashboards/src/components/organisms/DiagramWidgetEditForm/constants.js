// @flow
import {FIELDS as BASE_FIELDS} from 'containers/WidgetEditForm/constants';

const colorsSettings: 'colorsSettings' = 'colorsSettings';

const FIELDS = {
	...BASE_FIELDS,
	colorsSettings
};

export {
	FIELDS
};
