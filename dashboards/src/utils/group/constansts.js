// @flow
const DAY: 'DAY' = 'DAY';
const MONTH: 'MONTH' = 'MONTH';
const QUARTER: 'QUARTER' = 'QUARTER';
const SEVEN_DAYS: 'SEVEN_DAYS' = 'SEVEN_DAYS';
const WEEK: 'WEEK' = 'WEEK';
const YEAR: 'YEAR' = 'YEAR';

const INTERVAL_MINUTE: 'INTERVAL_MINUTE' = 'INTERVAL_MINUTE';
const INTERVAL_HOUR: 'INTERVAL_HOUR' = 'INTERVAL_HOUR';
const INTERVAL_DAY: 'INTERVAL_DAY' = 'INTERVAL_DAY';
const INTERVAL_MONTH: 'INTERVAL_MONTH' = 'INTERVAL_MONTH';

const OVERLAP: 'OVERLAP' = 'OVERLAP';

const DATETIME: 'dateTime' = 'dateTime';
const DT_INTERVAL: 'dtInterval' = 'dtInterval';

const DATETIME_VARIANTS = {
	DAY,
	MONTH,
	QUARTER,
	SEVEN_DAYS,
	WEEK,
	YEAR
};

const DEFAULT_VARIANTS = {
	OVERLAP
};

const INTERVAL_VARIANTS = {
	INTERVAL_DAY,
	INTERVAL_HOUR,
	INTERVAL_MINUTE,
	INTERVAL_MONTH
};

const GROUP_TYPES = {
	DATETIME,
	DT_INTERVAL
};

const DATETIME_FORMAT = 'DD.MM.YYYY HH:mm';

export {
	DATETIME_FORMAT,
	DATETIME_VARIANTS,
	DEFAULT_VARIANTS,
	INTERVAL_VARIANTS,
	GROUP_TYPES,
	OVERLAP
};
