// @flow
import {createOrdinalName, createRefName, getMainOrdinalNumber, getNumberFromName, getValue} from './core';
import NewWidget from './NewWidget';
import {WIDGET_VARIANTS} from './constants';

export {
	createOrdinalName,
	createRefName,
	getMainOrdinalNumber,
	getNumberFromName,
	getValue,
	NewWidget,
	WIDGET_VARIANTS
};
